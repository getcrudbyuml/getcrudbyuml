CREATE TABLE `software` (
`id`	INTEGER PRIMARY KEY AUTOINCREMENT,
`nome`	TEXT
);
CREATE TABLE `objeto` (
`id`	INTEGER PRIMARY KEY AUTOINCREMENT,
`nome`	TEXT
);
CREATE TABLE `atributo` (
`id`	TEXT,
`nome`	TEXT,
`tipo`	TEXT,
`indice`	TEXT
);
